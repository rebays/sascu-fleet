# Base URL of the backend API (see /backend)
NEXT_PUBLIC_API_URL=http://localhost:5000/api

# Vercel Blob (for vehicle image uploads via /api/upload)
BLOB_READ_WRITE_TOKEN=vercel_blob_rw_xxxxxxxxxxxx
